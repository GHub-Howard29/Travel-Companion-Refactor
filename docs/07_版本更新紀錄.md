# Changelog

## v3
狀態:尚未開始，始於規劃階段

開始：


內容：
- 新增功能:
1.增加在旅費記帳本上面新增一個分頁：參考資料
裡面可能需要放置該行程的相關資料，例如伴手禮哪裡買，買哪種好，或是如何蒐集御朱印或是哪裡有怎麼問等等，這樣要如何設計分頁內容，才不用每個行程蒐集完資料後，還要再排版放到參考資料內，我初步想法是，參考資料裡面放的是網頁連結資料，點了後，會自動連結到放置的網址打開相關文件，例如word或是pdf，或是網頁，這樣我在製作參考資料頁面時，只要放入文件名稱跟連結網址即可，不知道這樣的設計合不合理，好不好用
2.增加分頁:
1.

- UI調整
1.帳本新增輸入時須可以選擇輸入修改記帳日期(預設為系統當天日期)，帳本清單需要依照建立日期作分頁篩選(格式:MM/DD)，避免全部都在同一個頁面下面，且清單須顯示記錄日期(格式:MM/DD)，位置就在帳本清單支出項目(每一筆的第一行)的最右手邊（靠右對齊，格式為：MM/DD/HH:mm）此欄位也要加入excel下載欄位內(記帳日期)，放在檔案內下載網址的左邊一格
2.目前已附加檔案編輯時，無法選擇刪除，只能選擇新的檔案覆蓋，這裡要新增可以刪除附加檔案的UI，刪除時不能動到原本紀錄
---支出項目清單(這裡已經有可以整筆刪除的按鈕了)
 |-新增狀態下，附加照片後，已經有可以"移除照片附件"的UI跟功能了
 |-記帳後，編輯時除了可以選擇新附加檔案覆蓋舊檔案以外，這裡要可以刪除附加檔案(跟新增狀態下的"移除照片附件"的UI跟功能一樣)
 |-紀錄後，清單項目如果有附加檔案，維持超連結，但是新指向路徑須更新到新的照片，如果已無附加檔案，需自動變更成純文字
3.帳本已記錄清單排列方式調整:目前是最舊的在最上面，最新的一筆放在最下面，我想顛倒過來，變成最新一筆放在清單顯示的最上面，下一筆資料顯示的是次舊紀錄，最下面為最新紀錄的那一筆

- 已知問題除錯
1.手機上面點選附加檔案的時候只有出現拍照功能並沒有選擇檔案照片的選項，但在電腦上反而跳出選擇檔案，並沒有拍照能，可能要檢查程式分析設備或是預設選項的程式代碼
2.檢查清單未做持久化設計(F5後會消失已經勾選的項目)，且會互相干擾其他行程檢查清單內容及進度

---

## v2
狀態:已完成，已佈署
Architecture Refactor

開始：

2026/07

內容：

- 模組化
- 重構
- 穩定同步

---

v1
狀態:已備份封存
GitHub Pages 初版

完成：

2026/06

內容：

- 基本記帳
- 共用帳本
- Excel
- 離線模式