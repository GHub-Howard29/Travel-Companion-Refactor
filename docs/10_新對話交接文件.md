# 新對話交接文件

> Travel Companion V3-1
>
> 最後更新：2026-07-10

---

# 一、目前狀態

本專案目前在 V3-1 階段。

已完成：

- Folder 架構定案為純 Tree model。
- 其他資訊 / 參考資訊第一階段本機管理。
- 共同檢查清單本機持久化與權限控制。
- 私人確認清單本機版。
- 私人確認清單最小雲端同步。
- 共同檢查清單最小雲端同步。
- Supabase checklist schema / RLS 已執行並人工確認。
- Trip 管理第一階段大架構調整：
  - App 內新增 / 編輯旅程基本資料。
  - 旅程清單改為 JSON seed + Supabase trips + localStorage fallback 合併。
  - `super_admin` 可新增旅程。
  - `trip_editor` 可編輯被指派旅程。
  - 可編輯者 Email 透過 `admin_users` 管理。
  - 已新增 Trip cloud schema / validation SQL，尚待 Product Owner 於 Supabase SQL Editor 執行。

最新功能 commit：

```text
0532277 完成共同清單同步與提交規範更新
```

---

# 二、已完成重點

## 共同檢查清單

目前使用：

```text
Trip JSON checklistData
↓
ChecklistPage
↓
useChecklistState
↓
sharedChecklistCloudService / checklistService
↓
checklistStorage
↓
Supabase + localStorage fallback
```

目前能力：

- App 第一次進共同清單時，若 Supabase 沒有 `scope = shared` checklist，會用 Trip JSON `checklistData` 初始化雲端共同清單。
- 共同清單優先讀 Supabase shared checklist。
- `trip_editor` / `super_admin` 可勾選並同步 `is_checked`。
- `guest` / `user` 可見不可勾選，且永遠看到乾淨未勾選版本，方便自行對照準備。
- 若雲端 shared checklist 已存在但缺少 Trip JSON seed item，`trip_editor` / `super_admin` 進頁時會補齊缺少 rows。
- 暫時不支援共同項目 App 內新增 / 編輯 / 刪除。

尚未完成：

- 共同項目新增 / 編輯 / 刪除。
- Checklist Pending Queue。
- Conflict Resolution。

---

## 私人確認清單

目前已完成：

- 入口位於共同檢查清單下方。
- Guest 不顯示入口。
- 本機 ownership 使用 `userEmail + tripId`。
- 可新增、編輯、刪除、勾選。
- 可顯示私人準備進度。
- 編輯時支援 Enter 儲存、Escape 取消。
- React StrictMode 下不會重複新增項目。
- 登出或未登入停在私人清單 / 記帳本時會導回行程頁。
- `trip_editor` / `super_admin` 可同步自己的私人清單到 Supabase。
- `super_admin` / `trip_editor` 不可讀取其他使用者私人清單。
- 雲端 item 以 `client_item_id` 對應本機 `private_...` item id。
- UI 顯示同步中、已同步、同步失敗。
- 第一次進私人清單時，若雲端私人清單已建立但沒有內容，顯示「雲端私人清單已準備好，新增項目後會同步」，不顯示同步失敗。

私人確認清單資料流：

```text
PrivateChecklistPage
↓
usePrivateChecklistState
↓
privateChecklistService / privateChecklistCloudService
↓
localStorage + Supabase
```

---

# 三、Supabase 狀態

已執行：

```text
docs/sql/001_checklist_cloud_schema.sql
```

已驗證：

```text
docs/sql/002_checklist_cloud_validation.sql
```

Product Owner 已人工確認：

- `checklists` / `checklist_items` table 存在。
- 兩張 table 的 RLS enabled。
- policies 已建立。
- helper functions 已建立。
- indexes 包含 `checklist_items_one_client_item_per_checklist`。
- `admin_users` 有 `email`、`role`、`trip_id`。
- 私人清單資料已可同步到 `checklist_items`。
- 共同清單資料已可同步到 `checklist_items`。

SQL Editor 的 query 草稿可刪除，但 repo 內 `docs/sql/*.sql` 必須保留。

待執行：

```text
docs/sql/003_trip_cloud_schema.sql
docs/sql/004_trip_cloud_validation.sql
```

`003_trip_cloud_schema.sql` 會建立 `trips` table、Trip RLS（Supabase 資料列權限，用來限制誰能讀寫特定 Trip 或使用者資料）、`admin_users` 管理 policy 與必要 index。

`004_trip_cloud_validation.sql` 用來確認：

- `trips` table 存在。
- `trips` RLS enabled。
- `trips` select / insert / update / delete policies 存在。
- `admin_users` 權限管理 policies 存在。
- `trips_touch_updated_at` trigger 存在。
- `anon` / `authenticated` grants 符合前端需求。

---

# 四、下一步

下一步請接：

```text
Supabase Trip Cloud schema 執行與實機驗證
```

建議範圍：

1. 在 Supabase SQL Editor 執行 `docs/sql/003_trip_cloud_schema.sql`。
2. 執行 `docs/sql/004_trip_cloud_validation.sql` 並確認結果。
3. 用 `super_admin` 登入測試 App 內新增旅程。
4. 確認 `trips` table 有新增 row。
5. 確認 `admin_users` 有新增 / 移除 `trip_editor` row。
6. 用 `trip_editor` 登入測試可編輯被指派旅程但不可管理 editor 名單。
7. 用 Guest 測試仍可瀏覽旅程。

Trip Cloud 驗證完成後，再接：

```text
Other Info / 參考資訊權限過濾與雲端同步設計
```

暫緩：

- App 內共同項目管理。
- Checklist Pending Queue。
- Conflict Resolution。
- 新 Travel Tool。

---

# 五、重要檔案

Checklist：

- `src/components/ChecklistPage.tsx`
- `src/hooks/useChecklistState.ts`
- `src/services/checklistService.ts`
- `src/services/sharedChecklistCloudService.ts`
- `src/storage/checklistStorage.ts`
- `src/types/checklist.ts`

Private Checklist：

- `src/components/PrivateChecklistPage.tsx`
- `src/hooks/usePrivateChecklistState.ts`
- `src/services/privateChecklistService.ts`
- `src/services/privateChecklistCloudService.ts`
- `src/storage/privateChecklistStorage.ts`

Permission / Workspace：

- `src/permissions/permission.ts`
- `src/permissions/roles.ts`
- `src/permissions/roleMapper.ts`
- `src/hooks/useTripWorkspace.ts`
- `src/App.tsx`
- `src/components/layout/AppSidebar.tsx`

SQL：

- `docs/sql/001_checklist_cloud_schema.sql`
- `docs/sql/002_checklist_cloud_validation.sql`
- `docs/sql/003_trip_cloud_schema.sql`
- `docs/sql/004_trip_cloud_validation.sql`

Trip Management：

- `src/components/TripEditorModal.tsx`
- `src/services/tripRepository.ts`
- `src/services/tripCloudService.ts`
- `src/storage/tripStorage.ts`
- `src/hooks/useTripWorkspace.ts`

交接 / 決策：

- `agent.md`
- `docs/001 V3-1_Handoff.md`
- `docs/002 V3-1_Architecture_Decisions.md`
- `docs/09_待辦事項(TODO).md`

---

# 六、驗證

目前最近一次驗證：

```bash
npm run lint
npm run build
```

皆通過。

Vite 仍有既有 chunk size warning，非目前阻塞項目。

---

# 七、開發規則

- 平常小步實作不逐次更新文件。
- Product Owner 要求整理文件、整理交接文件、準備 commit 或要求 commit 時，再集中整理文件。
- 文件或程式修改完成且驗證通過後，Codex 直接建立 commit。
- commit message 使用繁體中文。
- commit 完成後，Codex 回報實際使用的中文 commit message。
- commit 前不可提交 `dist/`、`.env` 或 secrets，除非 Product Owner 明確要求。
- Codex 使用專業術語時，必須附上對應本專案功能的解釋。例如 `RLS` 要說明為「Supabase 資料列權限，用來限制誰能讀寫特定 Trip 或使用者資料」。
